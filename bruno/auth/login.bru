meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{base_url}}/auth/login/
  body: json
  auth: none
}

body:json {
  {
    "email": "pm@test2.com",
    "password": "Test1234!"
  }
}

script:post-response {
  bru.setEnvVar("token", res.body.token);
}
