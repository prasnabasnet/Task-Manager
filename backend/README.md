# Task-Manager Backend

Django REST Framework API for organizations, departments, projects, tasks, and threaded comments.

Run all commands from this directory (`backend/`).

## Quick start

```bash
uv sync
uv run python manage.py migrate
uv run python manage.py runserver
```

Bruno collection: `./bruno`  
Full monorepo docs: see the root `README.md`.
